<UserControl
  x:Class="Oscilloscope.ShowException"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Oscilloscope"
  mc:Ignorable="d"
  x:Name="Self"
>
  <Border
    Background="{DynamicResource RegionBrush}"
    CornerRadius="20"
    BorderThickness="1"
    BorderBrush="{DynamicResource BorderBrush}"
    Padding="20"
    Margin="20"
  >
    <DockPanel>
      <TextBlock
        Text="{Binding Message, ElementName=Self}"
        DockPanel.Dock="Top"
        HorizontalAlignment="Center"
        FontSize="32"
        TextWrapping="Wrap"
      />
      <Button
        DockPanel.Dock="Bottom"
        Style="{StaticResource ButtonDanger}"
        Content="确定"
        FontSize="32"
        Height="60"
        Click="ButtonClick"
      />
      <ScrollViewer Margin="0,20">
        <TextBlock Text="{Binding StackTrace, ElementName=Self}" TextWrapping="Wrap" />
      </ScrollViewer>
    </DockPanel>
  </Border>
</UserControl>
